{
  "name": "siaas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "sudo docker build . -t 'registry.hamptonmoore.com/siaas' && sudo docker push 'registry.hamptonmoore.com/siaas'"
  },
  "author": "Hampton Moore",
  "license": "MIT",
  "dependencies": {
    "ws": "^7.4.2"
  }
}
